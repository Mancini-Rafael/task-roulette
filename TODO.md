# Roulette — TODO

## Ship blockers (direct download / notarized DMG)

- [ ] Enroll in the **Apple Developer Program** ($99/yr); install a **Developer ID Application** cert.
- [ ] Create a notarytool credential profile in the keychain
      (`xcrun notarytool store-credentials "RouletteNotary" --apple-id … --team-id … --password …`).
- [x] **App icon** — roulette-wheel icon generated by `scripts/make-icon.swift`
      (`make icon`), wired into `Sources/Assets.xcassets`. Swap the renderer if you want
      different art.
- [ ] Replace the GitHub Sponsors placeholder handle in `Sources/Views/SettingsView.swift`
      (`your-handle`).
- [ ] Add a `LICENSE` file; decide open-source vs closed.
- [ ] Run `scripts/build-release.sh` with `TEAM_ID` and `NOTARY_PROFILE` set.

## Post-launch / nice to have

- [ ] **Sparkle** auto-update with a signed appcast.
- [ ] Enable **App Sandbox** entitlement (deps are sandbox-compatible).
- [ ] Tune reel feel (`duration`, `cellWidth`, `winnerIndex` in `SpinReelView` / `SpinView`).

## Validation (do this BEFORE sinking time into distribution)

- [ ] Dogfood daily for ~1 week. Confirm the spin loop actually reduces task-paralysis
      before investing in the release pipeline.

## Deferred product ideas (not v1)

- [ ] Staleness-weighted picks (older un-picked tasks get higher weight).
- [ ] Time-available / energy filters for stronger eligibility.
- [ ] iCloud / CloudKit sync; mobile companion.
- [ ] Reminders / notifications.
